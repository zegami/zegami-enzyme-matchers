/**
 * @providesModule Matcher
 * @flow
 *
 * Used in Enzyme Matcher Objects (Jest)
 */

export type Matcher = {
  pass: boolean,
  message: string,
  negatedMessage: string,
  contextualInformation: {
    actual?: string,
    expected?: string,
  },
};
